# Route for raidboss app
apiVersion: cloudstate.io/v1alpha1
kind: Route
metadata:
  name: raidboss-routes
spec:
  http:
  - name: raidboss-routes
    match:
    - uri:
        prefix: /raidboss/
    route:
      service: raidboss-service
